server.port=8080
spring.application.name=${project.name}
logging.file=\${user.home}/log/\${spring.application.name}-\${server.port}.log

# 运行环境必须的编码设置
spring.mandatory-file-encoding=UTF-8

version.group=${project.group}
version.artifact-id=${project.name}
version.version=${project.version}